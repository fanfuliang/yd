<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yudao.lianying.v1.basicConfig.dao.mapper.LanguageMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.yudao.lianying.v1.basicConfig.dao.Language">
        <id column="keyid" property="keyid" />
        <result column="name_zh" property="nameZh" />
        <result column="name_en" property="nameEn" />
        <result column="code" property="code" />
        <result column="remark" property="remark" />
        <result column="pinyin" property="pinyin" />
        <result column="lang_family_id" property="langFamilyId" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        keyid, name_zh, name_en, code
    </sql>

    <select id="getQuickTranSourceLang" resultMap="BaseResultMap">
        select keyid, name_zh, name_en, code, pinyin, lang_family_id
        from t_language
        where `status` = 'T' and (`purpose` &amp; 2=2);
    </select>
    <select id="getQuickTranTargetLang" resultMap="BaseResultMap">
        select keyid, name_zh, name_en, code, pinyin, lang_family_id
        from t_language
        where `status` = 'T' and (`purpose` &amp; 4=4);
    </select>
    <select id="getAccountLangAbilityList" resultMap="BaseResultMap">
        select keyid, name_zh, name_en, code, pinyin, lang_family_id
        from t_language
        where `status` = 'T' and (`purpose` &amp; 8=8);
    </select>

    <select id="getLang" resultMap="BaseResultMap">
        select keyid, name_zh, name_en, code, pinyin, lang_family_id,remark
        from t_language
        where `status` = 'T'
        <if test="null != purpose">
            and (`purpose` &amp; #{purpose}=#{purpose});
        </if>
    </select>

    <select id="selectListByIds" resultType="com.yudao.lianying.v1.basicConfig.dao.Language">
        select keyid, name_zh nameZh,name_en nameEn, code from t_language
        where `status` = 'T' and FIND_IN_SET(keyid, #{ids}) != 0
    </select>
    <select id="selectNamesByIds" resultType="java.lang.String">
       select GROUP_CONCAT(name_zh) from t_language
        where `status` = 'T' and FIND_IN_SET(keyid, #{ids}) != 0
    </select>
    <select id="getId" resultType="java.lang.Integer">
        select  max(CONVERT(keyid,SIGNED))+1 from t_language
    </select>

    <update id="modifyByIdAuto">
        update t_language set
        <foreach collection="modifyFieldNameArray" item="moidfyFieldName" separator="," index="index">
            ${moidfyFieldName}= #{modifyFieldValueArray[${index}]}
        </foreach>
        where keyid in
        <foreach collection="idList" index="index" item="id" separator="," open="(" close=")">
            #{id}
        </foreach>
    </update>


</mapper>
